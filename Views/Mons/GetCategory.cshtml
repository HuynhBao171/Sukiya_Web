@model IEnumerable<Sukiya.Models.LoaiMon>
<h1>Loại Món</h1>
<ul>
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.TenLoaiMon, "GetFoodByCategory/" + item.MaLoaiMon, "Mons")</li>
    }
</ul>

